#include "structCellule.h"

/*
 * Création d'une structure "cellule" qu'on affecte a chaque case de la grille afin de leur définir :
 * - un état (bool) :
 *     - true (vivant)
 *     - false (mort)
 * - un nombre de voisin (unsigned)
 */
struct cellule;
